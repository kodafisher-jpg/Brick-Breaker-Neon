import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kodaf.neonbrickbreaker',
  appName: 'Neon Brick Breaker',
  webDir: 'dist'
};

export default config;
